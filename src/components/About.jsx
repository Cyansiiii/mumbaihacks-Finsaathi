import gsap from "gsap";
import { useGSAP } from "@gsap/react";
import { ScrollTrigger } from "gsap/all";

import AnimatedTitle from "./AnimatedTitle";

gsap.registerPlugin(ScrollTrigger);

const About = () => {
  useGSAP(() => {
    const clipAnimation = gsap.timeline({
      scrollTrigger: {
        trigger: "#clip",
        start: "center center",
        end: "+=800 center",
        scrub: 0.5,
        pin: true,
        pinSpacing: true,
      },
    });

    clipAnimation.to(".mask-clip-path", {
      width: "100vw",
      height: "100vh",
      borderRadius: 0,
    });
  });

  return (
    <div id="about" className="min-h-screen w-screen">
      <div className="relative mb-8 mt-36 flex flex-col items-center gap-5">
        <p className="font-general text-sm uppercase md:text-[10px]">
          Welcome to Finsaathi
        </p>

        <AnimatedTitle
          title="Trans<b>f</b>orm  Chaos <br /> Into Financial <b></b>Clarity"
          containerClass="mt-5 !text-black text-center"
        />

        <div className="about-subtext">
          <p>Watch AI Transform Your Financial Chaos</p>
          <p className="text-gray-500">
            Our AI engine processes your earnings data in real-time, 
            identifies hidden patterns, and automates actions to build your financial future.
          </p>
        </div>
      </div>

      <div className="h-dvh w-screen" id="clip">
        <div className="mask-clip-path about-image">
          <img
            src="public/image/task_01kb62g841f8ra4s64jv7c8twp_1764361918_img_0.webp"
            alt="Background"
            className="absolute left-0 top-0 size-full object-cover"
          />
        </div>
      </div>
    </div>
  );
};

export default About;
